# Puerto (Railway lo setea automáticamente — no hace falta cambiarlo)
PORT=4000

# Ambiente
NODE_ENV=production

# Origen permitido (opcional — Railway ya acepta *.vercel.app automáticamente)
# Si usás un dominio personalizado, agregalo acá separado por comas
ALLOWED_ORIGIN=https://tu-app.vercel.app
